info:
  name: API/hello
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:9080/api/hello/seakeen
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
